{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default capability for the main panel window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
