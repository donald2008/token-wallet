# 人工审核意见 — t_60ab5e19

reviewer: njbx02 (老大终审, human 阶段)
verdict: **approved**
commit: a254dbe8fb8fe22002bf5c927d4bce3bd1f2cd87 @ feat/theme-glass
base: a2589f2 (t_2ca0af5e 终审基线)
date: 2026-09-05

## 裁决

APPROVE。360px 标题栏默认态截断 P0（t_2ca0af5e 唯一 BLOCKING 打回项）修复成立，
**发版门禁解除**（feat/theme-glass 汇流/发版不再被本项阻塞）。

## 独立验证（njbx02 审查环境，非 worker 自报转述）

| 项 | 结果 |
|---|---|
| git 证据链 | `git fetch` 实核 origin/feat/theme-glass HEAD == a254dbe，本地 ff 拉到 |
| diff | 3 文件 +23/-10（app.css / TitleBar.test.tsx / information-architecture.spec.ts），scope 零扩散，排序/置顶/底边栏零触碰 |
| 数值纪律 | 本 commit 未改任何尺寸数值（删的 min-width:0/overflow:hidden/text-overflow:ellipsis 非尺寸值），8px 网格与 token 语义保持 |
| typecheck | tsc 0 errors |
| vitest | 330/330 |
| e2e | 85/85（43.5s） |
| build | vite exit 0 |

### 独立截图复核（老大自采，非 worker 图）

360×600 生产规格 viewport + DSF=1，dev server 生产 CSS，dark/light/dark-glass 三主题：

- `.app-title` scrollWidth == clientWidth == 71（三主题一致），标题右缘 x≈107
- 5 个图标钮 [34,34,32,31,29]px，最右钮右缘 x=340 ≤ 360
- docOverflow = 0
- vision 逐图复核：「token-wallet」完整、无省略号、5 钮全在窗口内 ✓

### e2e 证伪（断言有效性实证）

1. 仅加回 ellipsis 三件套 → 测试仍绿（预期：空间够就不触发截断，断言语义=「是否真被裁」而非「CSS 有无 ellipsis」）✓
2. 加回 ellipsis + 标题文本加长 4 倍（真溢出）→ 新断言在 spec L278 **变红** ✓ → 恢复现场

### Kimi 卡第二配额行（顺带项）

360×600 下 Kimi 卡 scrollIntoView 后卡体 y 340..448，两配额行（5h 82% / 周窗 17%）完整在视口内，
vision 复核确认 —— 此前观察到的「裁切」确为滚动位置假象，非真裁切。✓

## 已知风险记录（p2，非阻塞）

删 overflow:hidden 后，未来标题栏按钮 +1 导致超预算时标题将视觉外溢（非省略号），
运行时无防护，依赖 e2e `docOverflow <= 0` fail-loud 兜底。属契约加固取向，认可 worker
注释明示的设计取舍。**未来标题栏加按钮时若 e2e 红，须收敛预算而非恢复 silent ellipsis。**

## 修复手法评价

「删 silent ellipsis 兜底 + CSS 契约断言翻转（强制→禁止）+ e2e scrollWidth≤clientWidth 硬断言」
三件套闭环：改动、机制、断言、数据证据链全部静态闭环。契约缺口（toHaveText 对 CSS ellipsis
无效）已由 layout API 断言补上，并证伪有效。
