# 人工审核意见 — t_034a6e81

reviewer: njbx02 (老大, human 终审阶段)
verdict: approved
round: 3 (承接 round-1 CHANGES_REQUESTED 2B+3W → round-2 auto-review APPROVE)
head_sha: 21c2002e1c1accc26e186204875201f8408eef78
base_sha: a8ef35b0cb6143d40e8c4e54443d4bdaa77d836c
branch: feat/theme-glass

## 独立验证（njbx02 干净 clone @21c2002, 非 worker 工作区）

- git 三查: commit 8 files / +415 / -11（round-1 B1 漏提交的 ProviderCard.test.tsx 已 amend 纳入）; `git ls-remote origin feat/theme-glass` == 21c2002 ✓; base a8ef35b 为 ancestor ✓
- `pnpm -r typecheck`: 0 errors (core + app)
- `pnpm -r exec vitest run`: core 16 files / 165 tests + app 34 files / 355 tests 全绿（与 round-2 自报 355 对齐, W1 数字偏差已清）
- `pnpm -C packages/app test:e2e`: **104 passed (1.1m)**（亲跑落数字）
- 冷读 diff（contract lens, 对照原始卡体逐项）:
  - Bug1: OneClickAuth done 态 onClick = `onRefresh?.(providerId)` 不再走 onStart; 预览卡（无 onRefresh）done 态 disabled + title/aria（zh+en i18n 双键齐）; App.tsx 照 onDelete 的 realInstanceIds.has 三元模式同构 ✓
  - Bug1 测试: vi.mock ipc 真流程 idle→starting→waiting→done, 断言 onRefresh('kimi-code') 被调 1 次且 commandAuthStart 次数未增（round-1 B2「只 idle 静态断言」已根治）; 反向对照 3/3 命中已落板 ✓
  - Bug2: SchedulerInstanceDef.channel 可选; channelInFlight Map + prev.then 双臂接链（fail-isolated）+ finally 链头清理; 无 channel 走原并行路径零变化; engine http/command 两路均透传 inst.channel ✓
  - Bug2 测试: deferred 句柄精确断言同 channel 严格 FIFO（a-start→a-resolve→b-start→b-resolve）、不同 channel 并行、无 channel 全并行、fail-isolated 4 用例 ✓
  - round-1 W2/W3 注释矛盾已改写, grep stale 措辞零命中 ✓

## 本机 arkcli 撞锁实证（验收 Bug2 第 3 项「本机验证」）

- 并发两条 `arkcli usage plan` → 双双报「另一个 arkcli 进程正在刷新 SSO 凭证，请稍后重试」= **用户复报的撞锁现象在 njbx02 本机精确复现**, 根因机制（同 channel 并发 → SSO 刷新互撞）实锤
- 串行验证受限: 本机 SSO 会话已过期（.sso-pending.json 停在 9/1, 需交互式 `arkcli auth login volc-sso`, headless 不可完成）→ 串行成功路径无法在本机闭环, 属环境限制非代码缺陷; 串行语义由 4 个单测（deferred 顺序断言）独立证明

## 验收清单核对

| 项 | 结果 |
|---|---|
| Bug1 已授权点击不再弹授权页（done→onRefresh） | pass（代码+真流程测+反向对照） |
| Bug1 预览卡 done 态禁用 | pass |
| Bug2 同 channel 串行/异 channel 并行单测 | pass |
| Bug2 现有 scheduler 测试零回归 | pass（165 core 全绿） |
| Bug2 本机撞锁验证 | 并发撞锁复现 ✓ / 串行成功路径受本机 SSO 过期限置（记档） |
| typecheck 0 / vitest 全绿 / e2e 全绿（亲跑） | pass（0 / 165+355 / 104） |
| commit + push + ls-remote 核对 | pass |
| 360×720 真机流程截图 | **deferred → 用户真机验收**（授权流需真实 bl/arkcli 凭据+浏览器, 环境能力矩阵: 涉真凭据/窗口观感一律不派 Linux lane; 随下次出包用户装机验收覆盖） |

## 观察项（不阻塞, 承接 auto-review P2/P3, 认可记档）

1. [engine.test.ts「只刷目标实例」测] SUGGESTION — 断言只验 commandRun 总次数增长, mock 固定 provider_id "x" 无法区分触发源; 后续可改按 mock.calls 里 instance.id 过滤
2. [engine.test.ts L387 fixture] SUGGESTION — secret placeholder 含 sk- 前缀, 建议改名 test-legacy-999 防安全扫描器误报
3. [scheduler.ts L222 注释] SUGGESTION — 「tick 自撞由 stats.skipped 自然吸收」措辞偏乐观: skipped 只吸收同实例重入, 同 channel 不同实例周期 tick 仍可能并发（串行链只护 refresh 路径）; 与用户复报场景无关, interval+jitter 下概率低, 记档不修

## 结论

round-1 全部 BLOCKING/WARNING 落地实证, 0 新增 P0/P1。APPROVED。
真机两项（arkcli 串行成功路径 + 360×720 授权流截图）随用户下次装机验收覆盖; 若真机复现撞锁或 done 态点击仍弹授权页, 重开卡。
