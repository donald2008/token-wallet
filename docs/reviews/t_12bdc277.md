# 人工审核意见 — t_12bdc277

reviewer: njbx02 (default 人工终审, execution lens)
verdict: changes_requested
date: 2026-09-11
branch: feat/mcp-server @ 894957c589dd13ce30c61e41d666d534cf794979 (gitee, ls-remote 一致)

独立复验环境：njbx02 干净 worktree（ext4, 大小写敏感）@ 894957c，
`pnpm install --frozen-lockfile` → `pnpm -C packages/core build` → 下述全部命令现场复跑，
不采信 worker 自报。

## 复验结论（大部分交付项成立）

| 项 | 结果 | 证据 |
|---|---|---|
| git 证据链 | ✓ | base faa7cce / eab3818 / head 894957c 三 hash `cat-file -t` 均=commit；`git ls-remote origin refs/heads/feat/mcp-server` = 894957c |
| typecheck | ✓ 0 错 | core + app 双包 `tsc --noEmit` 干净（首次失败是我侧未先 build core，补 build 后 0 错） |
| 单测 | ✓ | app 44 文件 535 全绿；core 16 文件 171 全绿 |
| e2e | ✓ | 全量 `playwright test --project=browser-only` = 121 passed；agent-card.spec.ts 9/9 |
| A arkParseOk 三层判据 | ✓ 含反向对照 | 我亲自摘掉 ② 分支 → `electron/auth-defs.test.ts` = **1 failed \| 17 passed**；恢复 → 18 passed。反向对照真咬 |
| B stripAnsi | ✓ | `\x1b\[[0-9;]*[A-Za-z]` 落在 URL 提取前 / parseOk 前 / 错误尾段前，同一干净文本判据+展示 |
| C cli_missing L1 | ✓ | spawn ENOENT + win32 包壳 `not recognized`/中文判别串双分支；stderr 不再 `() => {}` 丢弃（尾 300 字符进错误信息）；ProviderCard 渲染安装命令+复制按钮+重启提示；i18n zh/en 六键齐 |
| D PATH 自检 L2 | ✓ | `npm config get prefix` → 分号/冒号分隔符与大小写归一比较，失败优雅降级；只诊断不写注册表（符合卡面边界）；PowerShell 修复命令+复制按钮 |
| 边界 | ✓ | `git diff --name-only faa7cce..894957c` = 11 文件全在 packages/app；mcp-* / engine / volcengine-ark.ts 零触碰 |
| round-2 门禁修复本体 | ✓ | `App.tsx:493` `providers !== null &&` 语义正确（实证见下） |
| round-2 占位文案中性化 | ✓ 真咬 | 我把 i18n `local.body` 退回旧文案 → e2e 用例 9 立刻 fail（`即将推出` 断言不成立），恢复即绿 |

## BLOCKING

### B1 [packages/app/e2e/agent-card.spec.ts:236 与 :256] round-2 新增两条 e2e 对目标缺陷零判别力（改前改后都绿）

worker round-2 自报「零 provider 实例 + daemon ok → Agent 卡区仍可见 ✓ / 零实例 + unreachable → AgentCardEmpty ✓」，
但这两条用例在浏览器 e2e harness 里**根本没有走到 `providers.length === 0` 这条路径**：

- harness 跑的是 vite **DEV** 构建；`src/panelProviders.ts:21-22` 在零实例 + 非生产下
  `return scenarioProviders(opts.scenario)`，而 `App.tsx:106` 默认 scenario = `"mixed"`（`mockData.ts` 内含多张演示卡）。
- 于是「不 seed instances」≠「零 provider」：`providers.length > 0` 恒成立，
  两条用例断言的 `agent-card-section` 可见性与被修的门禁条件无关。

实测证据（njbx02 worktree，App.tsx 恢复为修复前 `{providers !== null && providers.length > 0 && (`）：

1. 打回门禁 + 跑 `agent-card.spec.ts` → **9 passed（9/9 全绿）**，包括 :236 / :256 两条「零实例」用例。
2. 同一打回门禁 + 我补的判别探针（点 `scenario-empty` 让 dev 下 providers 真为 `[]`，断言 `empty-state` 可见后
   `agent-card-section` 仍可见）→ **1 failed**：`getByTestId('agent-card-section')` element not found。
3. 恢复修复后代码 + 同一探针 → **1 passed**。

即：修复本体是对的（生产零实例下 Agent 区此前确实被门禁藏掉，修后可见），但新增用例是**空断言**——
它保护不了自己声称保护的门禁，将来任何人把 `providers.length > 0` 写回去，测试仍全绿。

要求（最小改动，一处）：
- 两条 round-2 用例改为在 dev harness 里真正构造 `providers = []`：先同意隐私声明 → `seedAgentUsage(...)` →
  `page.reload()` → 点 `scenario-empty`（`data-testid="scenario-empty"`）→ 断言 `empty-state` 可见
  → 再断言 `agent-card-section` 可见 / `agent-card-empty` 显式 reason（原 `agent-empty-reason` 文案断言保留）。
- 交付时附反向对照证据：把 `App.tsx` 门禁改回 `providers.length > 0 &&` 时这两条**必须红**，恢复后绿（同 A 项口径）。
- 卡面「单测：零实例 + mcpSummary.ok → agent-card-section 可见且含 AgentCard」按此口径重算，自审报告里
  「e2e 121 全绿」需要区分「绿」与「真咬」。

## WARNING / SUGGESTION（不阻断，随修完一起记档即可）

- W1 [e2e 语义] `agent-card.spec.ts` 中「零 provider 实例」措辞在 dev harness 下名不副实
  （scenario mock 会补卡）。建议注释里写明「dev 下零实例 ≠ 零 provider，需切 scenario=empty 才是真零」，
  避免后续审查者/worker 再次误采信。
- S1 (worker 自报，保留) `auth-session.ts:117 isWinShellCommandNotFound` 与 `core adapters.ts:74 isShellCommandNotFound`
  判别串重复，后续统一到 core 单源（本卡边界禁碰 core，不在此修）。
- S2 (worker 自报) `detectPathHint(_command)` 第一参数未使用，可改名 `detectNpmPrefixInPath()` 让语义直白。
- S3 (worker 自报) PowerShell 修复命令为 win32 专属文案，macOS/Linux 分支留将来扩展点。
- S4 [fixture 保真度] `auth-defs.test.ts` 的 1.0.27 fixture 把真机原文的 ANSI 还原为 `\x1b[...` 并在
  「▶ 正在交换访问令牌…」后补了一个 `\x1b[0m`——关键 token（`{"auth_method":"sso_no_browser","path":"same"}` +
  「√ 火山 SSO 认证成功!」+「✓ 同账号 SSO refresh」）与卡面真机原文逐字一致，重置码位置属还原，
  不影响判据（判据与重置码位置无关），故不阻断；后续 fixture 建议只保留真机原样片段，不补码。

## 未亲验项（njbx02 侧无法复现，属用户真机验收）

- Windows 侧 arkcli 1.0.27 真机两连（njbx02 无该 CLI）：① 断开 SSO 再授权 → 卡显「授权成功」；
  ② 临时改 PATH 移除 arkcli → 卡显 cli_missing 引导而非「提前退出」黑话。
- 卡面验收项「用户真机复验」保持待用户执行；B1 修完后建议一并复验（两处改动同属 app 显示层，互不冲突）。

## 结论

交付主体（P0 三连 + L1/L2 引导 + round-2 门禁解绑/文案中性化）代码与证据链成立，唯 B1
（round-2 两条 e2e 对门禁零判别力）需按上口径补齐反向对照后才能收口。修完 commit + push
feat/mcp-server 后重新 request_review（provenance 会回到同一 reviewer）。
