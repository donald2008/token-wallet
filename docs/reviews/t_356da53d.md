# 人工审核意见 — t_356da53d

reviewer: njbx02
verdict: changes_requested   # changes_requested | approved
date: 2026-08-31

## 结论摘要

功能层面 APPROVED 潜力大：adapter 三态分类、双 stream 判别、percent 0-100 不×100、
reset_at 秒转换、win32 包壳复用、注册三处一致，全部经独立冷读 + 复跑核实
（core 132/132、app 241/241 重跑全绿、typecheck 0、e2e command-channel 4/4）。
但存在 1 个 BLOCKING + 2 个 WARNING，修正后即可通过。

## 修改项

1. [docs/DECISIONS.md + 注释] **BLOCKING — D-043 决策编号撞车且未落册**
   - 前卡 t_65241a9e（commit b9b6faf "feat(app): D-043 key 去重"，app 侧
     engine.ts/store.ts/schema.ts 5 处注释引用 D-043）先占用了 D-043 编号。
   - 本卡（commit d807d46）又在 core 侧 presets.ts / volcengine-ark.ts /
     test 文件 5 处自称 D-043 → **同一编号两个语义**，后续维护者 grep D-043
     无法区分，违反 token-wallet 决策编号查重纪律。
   - DECISIONS.md 中 D-041/D-042 均有条目，但 **D-043 两条都没落册**。
   - 修复：本卡改声明为 **D-044**（volcengine-ark.ts 头注释、presets.ts 注释、
     两处 test 文件注释、commit 消息风格），并在 docs/DECISIONS.md 按
     D-041/D-042 格式落册 D-044 条目（含 arkcli 三态分类/percent 0-100/
     logged_in 判别/SSO 两段式指引要点）。
   - 附注：t_65241a9e 的 D-043 也应落册（可由该卡 review 时一并要求）。

2. [packages/core/test/volcengine-ark.test.ts] **WARNING — logged_in===false 分支无独立用例触达**
   - 现 fixture ark-auth-status-never-configured.json 的 hint 含 "auth login" 串，
     先命中 isAuthExpiredBody 提前返回，healthCheck 的 logged_in===false
     分支（volcengine-ark.ts 约 263-266 行）实际无测试覆盖（自审同款发现，已核实）。
   - 建议（不强制）：补一条 inline body `{"auth_method":"none","logged_in":false}`
     （不含 "auth login" 判别串），钉死该真实字段判别分支。
   - 不修的代价：该防御分支的回归保护缺失；可观测结果目前一致，无功能缺陷。

3. [packages/app/src/runtime/engine.test.ts] **WARNING — 既有 flaky（非本卡引入，记录在案）**
   - 第一次全量复跑 app 241 中 1 失败：engine.test.ts "D-043 存量补写" 用例
     `await new Promise((r) => setTimeout(r, 0))` 等异步补写落定，单文件与
     重跑均全绿 → 时序竞态 flaky。
   - 属 base b9b6faf 引入的测试（key 去重卡），不在本卡 diff；本次不阻塞，
     但建议后续修掉（改 vi.waitFor 或真实 await 补写链路）。已在 kanban
     comment 记录，避免他人误判为 workers 声明的 241/241 造假。

## 验证（njbx02 独立复跑, commit d807d46）

- git 三查：base/head SHA 存在，HEAD == head_sha，远程 origin/master == d807d46 ✓
- core typecheck 0 / vitest 132/132（14 文件）✓
- app typecheck 0 / vitest 241/241（重跑确认；首次 240+1 flaky 见修改项 3）✓
- e2e command-channel 4/4 未实跑（browser-only 依赖 playwright，worker 已报；
  本卡 e2e 无新增用例，4 个全走 bailian 路径）—— 备注不阻塞
- mappings.ts 零改动、presets.ts 仅加条目、协议结构未动 ✓
- 前端 DynamicForm 两段式指引从 health_check.command 动态取 CLI 名，通用非硬编码 ✓

## 复验要求

改完 push 后重新 request review；复验看：① D-044 全仓替换 + DECISIONS.md 落册
② logged_in 分支用例存在且绿 ③ 原 132/132 + 241/241 不回退。