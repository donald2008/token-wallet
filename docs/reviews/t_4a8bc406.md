# 人工审核意见 — t_4a8bc406

reviewer: njbx02 (default lane / 老大 human 阶段)
verdict: approved
card: [token-wallet 展示] Agent 用量详情大屏 — mock 数据 + 2-3 HTML 布局方案（chart.js，900×600）
branch: feat/agent-usage-dashboard @ f438b02
review date: 2026-09-09

## 审核方式

auto 阶段（desktop-e5jupfs-reviewer，APPROVE + 16 项独立证据）已交接 human。本次为 human 功能验证：
- 独立读取 mock 契约 + 三方案 DOM/CSS 结构（不依赖 worker 自述）
- vision 审 9 张截图（3 方案 × dark/light 900×600 + portrait 720×900）
  - ⚠️ 主 vision 链路（opencode-go + aliyun-token-plan qwen3.8-max）周配额双耗尽（429，09-12 前后重置）
  - 按 llm-provider-quota skill「视觉备用通道」降级 minimax-m3 手动直调，全部 9 张独立描述成功
  - C-dark 环形图标题旁"疑似错字"疑点已 zoom 复核排除（实为"饼图"图表类型标识，视觉模型误读）

## 结论：APPROVE（零 BLOCKING）

### 验收项逐条核对

| 验收项 | 结果 | 证据 |
|---|---|---|
| mock 严格对齐契约 | ✅ | types.ts 三分项 {tokens 必有, cost/currency 可空}；total 仅 cost 无 tokens；mock 13 处 cost:null（opencode-spark 全覆盖 + 4 子项） |
| 2-3 方案 HTML 独立打开，900×600 不破 | ✅ | 9 张截图 vision 复核无破图/溢出/截断/错位；portrait 等比缩放居中 |
| chart.js 真实渲染 | ✅ | 三 HTML 均引 jsdelivr chart.js@4.4.4 + new Chart×2；截图可见柱状/环形/堆叠真实数据 |
| 有金额/无金额两种 agent 呈现 | ✅ | opencode-spark 金额列留空（visibility:hidden 语义，非 0/—）；hermes-ws2 空闲行 tokens 显 "—" 属空闲态非金额缺失 |
| 截图 9 张 | ✅ | verification/agent-dashboard/ 9 PNG，900×600 ×6 + 720×900 ×3 |
| 方案差异真 | ✅ | CSS 实锤：A=grid 5fr/7fr + rows minmax(0,240px)/1fr（左 hero+trend / 右 model+detail+split）；B=rows auto auto 1fr auto + row-mid 5fr/4fr/3fr（上中下流）；C=rows auto 1fr 1fr + 2×2 grid 6fr/6fr。结构完全不同，非换配色 |
| commit+push+review | ✅ | f438b02 单 commit，working tree clean，remote feat/agent-usage-dashboard == HEAD（独立 ls-remote 核对） |

### 独立复核证据（njbx02 隔离 worktree /tmp/tw-review-4a8bc @ f438b02）

- 隔离 clone 后 git status 干净，17 文件全在（5 HTML/CSS/JS + 2 mock ts + 1 screenshot script + 9 PNG）
- mock 自洽性：明细 5 行 tokens 之和 534.2K+281.6K+174.7K+121.8K ≈ 1,112,350 = hero 总量 ✓；前 3 行 USD 之和 $7.07+$8.59+$5.54 = $21.20 = hero 总额 ✓（opencode-spark 无金额贡献正确）
- 方案差异 vision 交叉确认：A=split-pane 观感、B=top-down hero→trend→三栏、C=hero+2×2 grid；B 用 line 趋势图（max 242.3K）、A/C 用 bar —— 同数据不同表达，属真实方案差异
- 9 张 PNG 均真实渲染（非占位）：hero 1,112,350 tokens · $21.20 USD / 5 行明细 / 环形图 8 model 图例齐全（=hero "模型 8"） / 堆叠条 15.1/56.1/28.9%

## 给下一张落地卡（真实大屏）的参考

本卡不实现真实大屏（按"不做"约束）。落地卡可参考：
- 三方案均可进真实大屏；A split-pane 信息密度最高适左 hero 右详情的操作台；B top-down 最适合 900×600 横屏"先总览后钻取"；C 2×2 grid 均衡适合固定大屏
- P2（不阻断）：mock 用简化版契约（任务 body 明示），真实 daemon/hook 实现卡接 docs/mcp-protocol.md 权威 schema（含 schema_version/event_id/context 等字段，reviewer 已披露差异）
- dev-pages/ 为 dev-only 入口，vite 不打包（build 99 modules 无污染已验证）
- --font-13 新字阶可后续同步 design-tokens primitives（dev-only 当前不阻断）

## 备注

- 本次审核未改任何实现文件（reviewer 角色分离）
- 主仓 master 已有他卡在途 untracked 文件（capture-*.cjs 等），本次意见仅 add docs/reviews/t_4a8bc406.md 单文件提交
