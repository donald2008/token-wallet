# 人工审核意见 — t_15397c99

reviewer: njbx02 (default 老大人工终审, run 1020, contract lens)
verdict: approved   # 0 BLOCKING + 4 WARNING + 2 SUGGESTION

审核对象: `feat/dashboard-redesign` tip = 920b0d1（base 029967d，5 commits：fba8805/d84b12c/9dc34b6/e38883b/920b0d1）
auto 阶段: round-1 REQUEST_CHANGES（1×P1 + 2×P2）→ round-2 APPROVE（修复核验通过）

## 独立复核（本机实测，不采信 worker/auto-reviewer 声明）

| # | 项目 | 手段 | 结果 |
|---|------|------|------|
| 1 | Git 证据链 | `git fetch` + `ls-remote origin feat/dashboard-redesign` | = 920b0d15b088367f7b61e8340055475df70e7074，与 submission head_sha 逐字节同 ✓；5 commit 逐条 `cat-file -t` = commit ✓；`diff --stat` = 14 文件（11 源码 + 3 截图），零越界 ✓ |
| 2 | typecheck | 干净树（临时移开兄弟卡未跟踪 spec）`tsc --noEmit` | **0 错误** ✓（上一次报红为 `e2e/_t7665c497-verify.spec.ts` TS6133，属兄弟卡在途残留，非交付物） |
| 3 | vitest 全量 | `corepack pnpm test`（tip 复跑 2 次） | 580 tests / 稳定红 1 条 = `add-kimi-flow`（kimi 窗口语义）；base 029967d 全量同样红该条 → **既有红，与本卡无关**；另 1 条 `ProgressBarTooltip` 24.0小时/1.0天 为时间边界 flake（复跑即过，实现注释自述 86399s 四舍五入边界） |
| 4 | e2e browser-only | 隔离端口 `PW_PORT=152x`（避免复用他人 dev server） | 134 passed / 3 failed / 5 skipped。归属：`badge-semantics.spec.ts:61` + `panel-semantics.spec.ts:145` 在 base 029967d **同样 2 failed / 6 passed**（逐条对照）→ 既有红；`_t7665c497-verify.spec.ts:68` = 兄弟卡未跟踪临时 spec；**本卡相关 spec `agent-card.spec.ts` = 24/24 passed** ✓ |
| 5 | design_lint.py | 仓根 `python3 packages/app/verification/design_lint.py` | exit 0 / 0 违规；打印值 vs `theme.css` 逐值吻合（dark fg 13.28 / accent 5.03 / light accent 4.65；ΔE 全 0.0）→ 真值解析（非硬编码快照）成立 ✓ |
| 6 | SC-09 独立复测 | 自建同源临时 spec（复制交付版 `agent-card.spec.ts` 追加断言+截图，跑完即删）900×640 三主题 | docOverflow=false；maxPanelBottom=546 ≤ 640；footer bottom=636 ≤ 640 ✓ |
| 7 | SC-01 视觉独立取证 | 同上三主题配色/结构直读（dark/light/dark-glass） | Model 面板 modelCount=2 + 环形 + 图例 + 中心总量在场；趋势 X 轴 = 中文星期（周二/周三）；明细 4 行（一行一 agent，含空闲态「—」）；三分项 / footer 完整 ✓ |

## 发现（0 BLOCKING）

**WARNING-1 明细表第 7 列 = 模型数，锁定参考稿第 7 列 = 成本**
- 证据: 交付渲染表头实测 = `[Agent, Tokens, 占比, Cache hit, Output, 调用, 模型]`（脚本机器读取）；参考稿 `50-design/ops-wall/index.html` th 第 7 = `成本`（`$24.40 / $13.10`）。
- 数据面可行: daemon `packages/mcp-server/src/mcp_server/tools_query.py`（行级 `cost_total` 实算）、`DetailRow.cost` 已接线、H3 只要求 null 留空不显 0 —— 即「金额列」并非数据不可为。
- 归因: 存量 029967d 已有同源注释「t_5cf22ba4(用户截图问题 5): pricing 未接入, 金额恒空 → 不展示空金额占位」→ 属**继承态**，非本轮新造；但本卡 submission / 两轮审查报告均未披露该偏离（round-1 报告称「appendix 逐块核对全过」）。
- 裁定: 不阻塞。要求 (a) 本文件即该偏离的落盘记录；(b) 若老大要求严格对齐锁定 mock 第 7 列，请在 SL-02/SL-05 开工前裁定（pricing 接入后恢复金额列，或金额 null 显「—」）；(c) SL-07 文档收口把「明细列集合」写进 frontend-AGENTS 面板语法节。

**WARNING-2 appendix DOM 契约的 `-detail-table` 未落地**
- appendix（实现基线）明写 `-detail-table`（原 detail-list **改名映射**）；实现仍是 `agent-dashboard-c-detail-list`（元素已由 `ul` 改 `table`，名未改）。
- `plan.md` 把「testid 映射落地」归 SL-05，故本卡保留现状可接受；但 SL-05 开工前必须把 22 项映射终态填进 `contracts/testid-contract.md` 并过审（plan R-1 已要求）——当前该表 22 项仍全「待定」，卡体 checklist「testid 映射逐条落实」目前只在 kanban 评论里成立、未落盘。
- 连带: B3 判 `hero-*` 命名「并入 kpi-*」，实现保留 `hero-tokens/hero-cost` 作为 `kpi-*` 内层锚（双 testid 并存、无裸删）→ 可接受的映射中间态，但须 SL-05 逐条记录；`calls/output` 两条 testid 已删（数据保留在 KPI 副行/模型表列）→ 同须在映射表补「删除+理由+断言同步」记录。

**WARNING-3 提交的 L2.5 证据覆盖不全**
- 交付的 3 张 900×640 截图中，Model 面板为空态「暂无模型数据」、趋势 X 轴为 agent 名（非日粒度）→ SC-01「常态全面板渲染」**未被提交证据覆盖**。我用同源重拍补齐（见下文产物），产品实际渲染正常，故属证据不足而非功能缺陷。
- 建议: 视觉卡 seed 必须覆盖常态全面板（summary + modelSummary + trendSummary 三维齐），截图与 e2e 同源且断言在 resize 之后。

**WARNING-4 分支基线既有红（非本卡，勿搭车修）**
- 2 条 e2e 红（provider 徽章健康度 bad/warn）在 base 与 tip 完全一致，且落在卡体明确划红的主面板域 → 与本卡无关。master 不可作对照基线（master 顶部有 mcp-server revert，同两 spec 失败更多）。建议独立排障卡处理。

**SUGGESTION-1 趋势柱顶数值标签**: 参考稿每柱有 `.bv` 数值，appendix 只要求「柱状 + 均值虚线 MEAN 标签」→ 不构成契约偏离；若要贴 mock 需引入 chartjs datalabels 插件（收益/成本请老大定）。
**SUGGESTION-2 lint ΔE 阈值 2.0** 与「series 逐字节一致」口径自洽（当前 ΔE=0）；SL 后续卡若走语义色映射该阈值会卡死（auto round-2 已记，转此存续）。

## 本次取证产物（不入 repo，供复核）

- `/root/reviews/t_15397c99/live-900x640-{dark,light,glass}.png` —— 900×640 三主题常态独立重拍（同源临时 spec：复制交付版 spec 追加断言+截图，跑完删除，未进仓）。
- 临时 spec 与工作树均已复原：受追踪改动 0，兄弟卡未跟踪产物原位未动。

## 收口

verdict: approved → `kanban_complete`，交接老大 GATE 3 真机验收（SL-06）。
下游约束: SL-05 开工前须先落 22 项 testid 映射终态并过审；SL-02/SL-05 处理 WARNING-1 列集合裁定结果。
