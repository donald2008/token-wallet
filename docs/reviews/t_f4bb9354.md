# 人工审核意见 — t_f4bb9354（SL-05 e2e 契约迁移收口）

reviewer: njbx02（老大 / human-stage 终审）
verdict: **approved**（0 BLOCKING · 2 WARNING · 1 SUGGESTION）
审核对象: `eddee07`（`feat/dashboard-redesign`，base `250038d`，diff = `packages/app/e2e/agent-card.spec.ts` +10/-0）
审核机: njbx02 · 干净 worktree `/root/work/tw-hr-t_f4bb9354`（detached @ eddee07）+ 隔离端口（PW_PORT=1573/1576/1577/1578/1579）
方法: sdlc-review Round 3 Contract 透镜 —— 回读原卡范围与契约表，逐项对照代码真值；不采信 worker/自审报告，全部亲跑。

---

## 1. Git 证据链（独立核验）

| 项 | 结果 |
|---|---|
| `git ls-remote origin refs/heads/feat/dashboard-redesign` | `eddee07` == 本地 HEAD == 卡上自报 hash ✓ |
| `git log 250038d..eddee07` | **仅 1 个 commit**（eddee07），线性无分叉 ✓ |
| `git diff --stat 250038d..eddee07` | 1 file changed, +10/-0（与 submission `changed_files` 一致）✓ |
| 越界改动 | 无（未混入兄弟卡在途产物；`_t7665c497-*` 等未跟踪 temp spec 不在本 commit）✓ |
| 凭据扫描（diff 内 password/secret/token/sk-/api_key） | 0 命中 ✓ |
| 交付工作树 | clean（`git status --porcelain` 空）✓ |

## 2. 范围 1 · 契约表逐项核对（grep 可验证）

契约基准 = `docs/requests/2026-09-18-dashboard-redesign/40-handoff/contracts/testid-contract.md` @ eddee07（35 项：旧 22 + 行级 1 + SL-01 新增 8 + SL-03 新增 3，SL-03 在 79f3f39 追加 3 项）。

- **实现层**：脚本化对照 `AgentDashboardC.tsx` + `App.tsx` 全部 `data-testid` 字面量与模板 —— 契约 35 项 **全部落实，FAIL 数 0**。
- **删除项零残留**：`agent-dashboard-c-calls` / `agent-dashboard-c-output` 全仓（排除 node_modules/.git/dist）grep **仅命中契约表「删除」行本身**，src/e2e 零引用 ✓（与契约「数据保留在 KPI 副行 + Model 表列」一致）。
- **`-detail-table` 改名否决落地**：`agent-dashboard-c-detail-table` 全仓 **0 命中**；`agent-dashboard-c-detail-list` 保留，DOM 为 table（e2e 4 处引用不变）✓。appendix `30-spec-appendix-req01.md:19` 的旧提议按卡体以契约表为准。

## 3. 范围 2 · e2e 断言映射完整性

e2e（`packages/app/e2e/*.ts`）引用的大屏域 testid 并集 = 42，与契约逐条对照 → **新增断言缺口仅 kpi-* 与 model-total**（banner-offline 已有 5 处，无需补），与 worker 判定一致。

**本卡新增 6 条断言，期望值全部由 fixture 现算复核（非采信注释）**：

| 断言 | 期望 | 独立现算（fixture 真值） | 结论 |
|---|---|---|---|
| `kpi-tokens` | 4,555,000 | 64,000 + 4,474,000 + 17,000 + 0 = 4,555,000（`fakeSummary` rows） | ✓ |
| `kpi-cost` | 13.57 USD | 1.23 + 12.34（home/desktop cost=null）= 13.57 | ✓ |
| `kpi-hit` | 80.0% | 3,370,760 / (3,370,760 + 842,690) = 80.0% | ✓ |
| `kpi-active` | 2/4 | activeCount=2（njbx02 + njbx02-heavy）/ detailRows.length=4 | ✓ |
| `model-total`（njbx02） | 128,000 | glm(88,000+14,000+3,000=105,000) + kimi(12,000+6,000+5,000=23,000) | ✓ |
| `model-total`（切 home 后） | 20,400 | glm(7,000+3,400+1,600=12,000) + deepseek(5,000+2,600+800=8,400) | ✓ |

**判别力突变测试（证断言非空转）**：在审机上对实现注入两处定向突变后复跑本域两用例 ——
① `model-total` 大数字改常量 `0` → 断言 FAIL（`Received string: "0TOTAL"`，spec:906）；
② `kpi-active` 分母改 `detailRows.length + 1` → 断言 FAIL（`Expected "2/4" / Received "2/5"`，spec:249）。
两处均按预期变红 → 新断言具备真判别力；突变已 `git checkout` 复原并与原始字节比对一致（`diff -q` 通过）。

## 4. 范围 3 · W4「既有红」两条归属裁定 → **销项，无需排障卡**

`badge-semantics.spec.ts:61` + `panel-semantics.spec.ts:145` 在**三个提交、干净 worktree、隔离端口**下全部复现为绿：

| 复跑点 | 命令 | 结果 |
|---|---|---|
| eddee07（本卡 tip） | `PW_PORT=1573 playwright test --project=browser-only` | **137 passed / 5 skipped / 0 failed**（两 spec 含在内，全绿） |
| 920b0d1（SL-01 tip） | `PW_PORT=1577 … badge-semantics panel-semantics` | **8 passed / 0 failed** |
| 029967d（SL-01 前 base） | `PW_PORT=1576 … 同上` | **8 passed / 0 failed** |
| 029967d **全量** | `PW_PORT=1578 playwright test --project=browser-only` | **135 passed / 5 skipped / 0 failed** |

→ 判定：`docs/reviews/t_15397c99.md:16` 记录的「base 029967d 同样 2 failed」**未能在干净环境复现**。实证差异源 = 当时测量工作树存在 3 个**未跟踪的兄弟卡 temp spec**（`packages/app/e2e/_t7665c497-shots.spec.ts`、`_t7665c497-verify.spec.ts` 等，在 /mnt/d 工作树至今为 `??`）+ 共享 dev server（`reuseExistingServer`）→ 判为**环境性假红**，非缺陷、非本卡漏修。**销项：不建排障卡**（与卡体「本卡不修，仅标注归属」的要求一致，且归属结论为「无待排障项」）。

## 5. 门禁独立复跑（本机 njbx02，亲跑非采信）

| 门禁 | 命令 | 结果 |
|---|---|---|
| typecheck | `pnpm -r typecheck`（先 `pnpm -C packages/core build`） | 0 错误 exit 0 ✓ |
| 单测 | `pnpm exec vitest run`（packages/app） | 45 files / **595 passed** ✓ |
| e2e 全量 browser-only | `PW_PORT=1573 pnpm exec playwright test --project=browser-only` | **137 passed / 5 skipped / 0 failed** ✓（5 skipped = electron-shell 平台守卫，Linux 正常） |
| 本域 spec | 计入上表全量；`agent-card.spec.ts` = 25 用例（22 个 test 定义含 1 处四主题展开）✓ | 与 worker 自报 25/25 一致 |

复跑后按惯例 `git checkout -- .` 复原被 e2e 覆盖写回的受跟踪截图（`packages/app/verification/*.png` 等）并清掉本轮生成的未跟踪 p5-home-* 截图，审机工作树恢复 clean。

## 6. WARNING（放行但记录）

1. **[报告数字不实]** 交付报告与 commit message 称「补断言 **8 条**」，`git diff` 实际为 **6 条断言语句**（kpi-tokens/cost/hit/active + model-total ×2），+10 行中 4 行为注释。报告自身明细也只列出 6 项 —— 数字虚高 2 条。属报告口径问题，不影响交付物。
2. **[契约表覆盖缺口，非本卡引入]** 卡体范围 2 要求「e2e 引用的全部 testid 与契约表逐一对应」，实测仍有 **6 项** e2e 引用的 testid 不在契约表：
   `dash-chrome` / `dash-chrome-min` / `dash-chrome-close`（`src/App.tsx:499/505/515`，standalone 无边框窗 chrome）、
   `dash-model-empty-retry` / `dash-trend-empty-retry`（`AgentDashboardC.tsx:260` 的 `` `${testid}-retry` `` 模板派生）、
   `agent-dashboard-c-model-row-{model}`（`AgentDashboardC.tsx:756` 行级模板）。
   报告里「全部逐一对应」「未列项…无越表新增」的结论对**本卡**成立（本卡零新增 testid），但把 `dash-chrome*` 归为「契约行级模板的实现形态」不准确 —— 它是另一组件域的窗口 chrome。
   *处置建议*：契约表补这 6 行，或在表头补一句范围声明（「本契约仅覆盖 AgentDashboardC.tsx 域；App.tsx 的 dash-chrome* 不在内」）。**不阻塞本卡**，可并入 SL-07 文档收口卡（见卡上 comment）。
3. **[记录]** 卡体前置把契约版本写成 f372062/920b0d1 的「27 项」，实际执行基准是 eddee07 的 35 项表（SL-03 于 79f3f39 追加 3 项降级态 testid）。worker 按最新表执行 = 正确处置，仅记录口径。

## 7. SUGGESTION

- `kpi-tokens` / `kpi-cost` / `kpi-hit` 三条是**父容器包含式**断言（其内 `hero-*` 已被 `toHaveText` 断言），判别力主要来自「契约新锚位存在」这一层；真正新增的独立覆盖是 `kpi-active`（含 `/{detailRows}` 分母）与 `model-total`（联动口径）。后续同类补断言优先选「无等价既有断言」的锚位，避免只加包裹层。

## 8. 裁定

范围 1/2/4 达成，范围 3 归属裁定为「环境性假红，销项」；两处 WARNING 均为报告/文档口径问题，无代码缺陷，**放行（approved）**。SL-07 文档收口卡（t_fe5efb40）随本卡 done 释放派发。
