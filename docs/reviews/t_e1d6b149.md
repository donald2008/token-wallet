# 人工审核意见 — t_e1d6b149

reviewer: njbx02 (default 人工终审, run 579)
verdict: changes_requested   # 1 BLOCKING 规范对齐 + 2 WARNING 记档

## 审核范围与独立复验（execution lens, 均本机重跑, 不采信 worker/auto-reviewer 声明）

- Git 三查: HEAD == origin/master == 02221fc0b69867074a3d1ffc44dad90be36bfc4c, 工作区 clean
- commit 链: a7158b9 → 1bd54d7(WebView2 bundle) → 02221fc(P0-3), 链完整无丢失
- diff 范围: 1bd54d7..02221fc, 仅 packages/app (13 文件 +444/-63), 与声明 scope 一致
- typecheck: tsc --noEmit EXIT 0 (pj 独立重跑)
- build: vite build 209.81KB JS, 与 worker 报告一致
- e2e: Playwright browser-only 5/5 全绿 (独立重跑, 6.6s)
- 约束: pnpm-lock.yaml grep chart.js/antd/mantine/@mui = 0 命中; 双 token 实测 (--warn ≠ --warn-fg, 浅色 #92600a)

## 验收项映射（全部实测通过）

| 验收 | 证据 |
|---|---|
| bars/ticker mock 正确渲染, 进度条+倒计时 | e2e #5: kimi 2 条 progress / bar-reset 含"重置" / ticker ¥ 数字 + 预计可用约 N 天 |
| 健康度排序最坏置顶, 颜色语义双 token | e2e #3/#4 + health.ts sortByHealth 三级键 (健康带→status 严重度→剩余比例) |
| 异常卡 黄灯/setup_hint/灰/红, 无假数据 | e2e #4: lamp 可见 + setup_hint 文案 + 异常卡 .progress/.ticker-number 计数为 0 |
| 全部 commit+push gitee master | ls-remote == 本地 HEAD, 链完整 |
| L2 Playwright 必带 (comment #709) | e2e 5 用例真实行为断言 (非快照) |

## 裁决: auth_expired 黄灯（确认 worker/auto 选择的镜头）

- 依据: 任务卡验收原文 "auth_expired 黄灯 + setup_hint" + DESIGN.md §2.1:47 "auth_expired(控制台类登录态断, UI 亮黄灯)"
- 语义: 登录态失效 = 可恢复的待处理告警 → 黄; 红留给 error/额度耗尽(恐慌态), 符合 glanceability
- P0-2 (59d1e06) 曾按 §9 实现恒红, 本次是真实跨卡语义变更 —— 变更本身正确, 但见 BLOCKING 1

## 修改项

1. [docs/DESIGN.md:346] BLOCKING — "auth_expired / 额度耗尽(100%) 恒为红, 不走阈值" 与已实现行为矛盾
   建议: 改为 "error / 额度耗尽(100%) 恒为红, 不走阈值; auth_expired 亮黄灯(待处理告警), 不走阈值", 与 §2.1:47 对齐
2. [docs/DECISIONS.md:28] BLOCKING — D-022 行 "auth_expired/耗尽恒红不走阈值" 同上
   建议: 改为 "error/耗尽恒红不走阈值; auth_expired 定黄(见 §2.1)"
3. [packages/app/src/types.ts:12] BLOCKING — HealthLevel 注释仍写 "红(低于红线或 auth_expired/耗尽)"，与本卡 self-diff 内新行为自相矛盾
   建议: 同步为 "红(低于红线、error 或耗尽) / 黄(auth_expired 待处理或低于黄线)"

## WARNING（放行但记录, 不阻塞）

1. [evid pack] base_sha 写 a7158b9 但 02221fc 实际 parent 是 1bd54d7 —— 证据采集偏差, 不影响结论 (实链已核)
2. [packages/app/src/components/ProgressBar.tsx] 进度条为 CSS div 而非 D-002 字面 "手绘 SVG" —— 手绘零依赖意图已满足 (lockfile 0 组件库/Chart.js), 与 P0-2 已批基线一致; 建议下次动文档时把 D-002 措辞放宽为 "手绘(HTML/CSS 或 SVG), 零依赖", 本卡不改实现

## 结论

代码质量为通过水准: 6 维设计原则检查 (SRP/SoC/DRY/OCP/状态机/硬编码) 全 PASS, 注册表模式(D-004)对后续 gauge/battery 扩展友好。唯一实质问题是**规范文档与实现的对齐尾巴** —— 按项目"设计-实现对齐铁律(每处偏离必须收拢)"打回, 修复量约 3 行文档/注释, 无需改任何逻辑。