# 人工审核意见 — t_5b092750

reviewer: njbx02(老大人工终审, round 1 · artifact lens + 独立复跑)
verdict: approved
date: 2026-09-07
base: 7619552 / head: 516c251(审核时 origin/feat/theme-glass 已前进到 fe56c03, P5 代码面无变化)

## 验收逐条

- [x] P5 渲染: 5h+周同一行两列、月全宽独立行 — 组件 `P5MonitorShortSide`(ProviderCardLayouts.tsx L254-291) 结构核对 + 三主题截图 vision 复核一致
- [x] **360px 硬门槛**: 三主题截图(dark/light/glass)我逐张 vision 复核——双列 quota-usage 窄列换行成两行("(80%)"折行)但**零重叠零裁切**, reset 文字与 usage 之间有净空; 月窗条横铺全宽。P3 被砍的撞字问题在 P5 两列(~144px/列)下由局部 CSS override 咬住, 成立
- [x] `.progress`×3 / `role=progressbar`×3 / testid 契约零破 — QuotaGallery.test.tsx P5 it 块(L224-253)断言覆盖, 独立复跑绿
- [x] 8px 网格 / tokens.css / D-016 三态: gap=var(--space-8), 行距 var(--space-4), 全部走 token, 无硬编码数值
- [x] typecheck 0 错 + vitest 336/336(34 files) — njbx02 干净 worktree @8805237 独立复跑(非信 worker 自报)
- [x] 截图 360×600 三主题 × P5 + 异常形态齐备(18 张入库)
- [x] commit 516c251 已 push, `git branch -a --contains` 确认在 origin/feat/theme-glass

## WARNING(放行但记录)

1. **[app.css L2039-2043] 窄列撞字修法是局部 override 的临时契约** — `.qcard3-windows-row:not(.qcard3-windows-row--wide) .quota-usage { overflow-wrap: anywhere }` 仅在 P5 双列容器内生效, 隔离正确(月窗全宽/P1/P2/P4 不命中, 未动 QuotaMeter 本体契约, 守住了卡体「不动 micro 本体」红线)。但若未来 micro 文案变长(新单位/新窗名), 360px 双列需重跑实测, 届时调选择器或文案, 不要再加第三层 override。
2. **[过程] P5MonitorShortSide 组件本体由兄弟卡 7619552(t_27eeadad)合入, 本卡只补 CSS/i18n/截图** — 该越界已在 t_27eeadad 终审意见 WARNING-1 记录, 本卡不重复扣分; 但「组件在 A 卡、样式在 B 卡」的拆法让两卡各自都不完整(7619552 时 P5 无样式=裸 DOM), 后续同仓串行卡应把「组件+样式+测试」放同一卡闭环。

## SUGGESTION

1. **[app.css L2028] 铁律③(显式对齐)未严格满足** — 新增 grid 容器 `.qcard3-windows-row` 只声明 display/columns/gap, 未显式 `align-items`/`justify-content`。现状依赖 grid 默认 stretch, 行为正确; 且同文件 `.qcard3-windows`(flex) 同样未声明, 属文件级惯例缺口而非本卡回归。建议后续统一补显式声明时一并处理, 不单独派卡。
2. **>3 窗 provider 的 P5 行为未定义** — 组件逻辑 `short = 非 monthly 全部进两列 grid`(第 3 个短窗会 wrap 到两列网格第二行, 与月窗行视觉混淆)。当前 mock 恒 3 窗, 方案页场景够用; 若 P5 未来接主页真数据, 需先定义 4+ 窗排版规则再接。

## 独立验证清单(njbx02 干净 worktree @8805237, 非 worker 机器)

- `git branch -a --contains 516c251` → 在 origin/feat/theme-glass ✓
- `corepack pnpm -C packages/core build` → ✓
- `corepack pnpm -C packages/app typecheck` → 0 错 ✓
- `corepack pnpm -C packages/app test` → 336/336(34 files) ✓
- 冷读 diff 7619552..516c251 全部 4 个代码文件 + P5 组件/测试全文 ✓
- P5 三主题截图逐张 vision 复核(独立于 worker 的 vision 结论) ✓
- 不改 P1/P2/P4、不动 ProviderCard/BarsTemplate/QuotaMeter 本体 — diff 面确认 ✓
