# 人工审核意见 — t_5d8c3c81

reviewer: njbx02 (default / 老大人工终审)
reviewed_head: d39dffce5be39da01ada8ee9bec05e1282329cac (origin/feat/theme-glass)
base_sha: c2e94823a0bff9b972a616ac5ce6d9e229be236c
verdict: changes_requested   # 1 BLOCKING + 1 WARNING + 2 SUGGESTION

独立复核（/tmp 隔离 clone @ d39dffc）：diff 冷读 6 文件 +589/-44 与证据一致；typecheck 0（pnpm -r typecheck，core 先 build）；vitest 39 files/436 passed 亲跑；e2e smoke+badge-semantics+panel-semantics 13/13 亲跑。engine 合并策略（ok 正常替换落库 / 失败+有旧 ok 保留骨架仅覆盖 status/alerts/error_message/setup_hint 且不落库 / 首次失败整卡 error / errorSnapshot 两调用点补 descriptor.logo）逐条对上卡体，测试反向对照扎实。

## 修改项

### 1. [ProviderCard.tsx:326-328 + i18n.ts:48/54] BLOCKING — 无旧数据 error 整卡仍渲染两行「采集失败」（验收「单行徽章」未达成）

现状：无旧数据（首次即失败/从未成功）error 卡 = head badge（410 行 `statusBadge` → badge.error=「采集失败」）+ AbnormalBody no-data 分支 abnormal-status-detail（327 行 → STATUS_DETAIL.error → statusText.error，zh 同为「采集失败」）。zh 下两键文案字面相同（en 才区分 Failed/Fetch failed），用户可见两行重复——正是本卡要修的缺陷在「整卡文字」形态的残留。

实证：临时渲染 error+metrics=[] 卡，全卡 textContent = `Kimi-Code #1采集失败采集失败上次更新: 1 分钟前`，「采集失败」出现 2 次（有旧数据形态实测 1 次正确）。

守卫漏洞：测试只数 `.card-status-text` class（126/130、658/661、695 行）——重复行被改名 abnormal-status-detail 后不再命中，断言全部假绿；且「error+无旧数据」用例（约 552 行）只断言 head badge 文案，不查卡内重复。e2e 同缺（badge-semantics 只覆盖 ok/auth_expired）。

建议（择一）：
- no-data 分支对 error（或更通用：STATUS_DETAIL 文案与 statusBadge 相同者）不渲染 abnormal-status-detail 行——错误原因已由下方 card-error-note/error_message 呈现，该行零增量信息；或
- 把 statusText.error 改为真正更长、不同于徽章的说明文案（如「采集失败，详见下方原因」）。
- 测试改为**文本级**守卫：error 无旧数据卡，`[...card.querySelectorAll('*')].filter(el => el.children.length===0 && el.textContent?.trim()==='采集失败')` 恰 1 个（不依赖 class 名），并给 e2e 补 error 整卡形态断言。用 class 名当防重复护栏 = 改名即破，不可接受。

### 2. [ProviderCard.tsx:300-316 + test:636] WARNING — auth_expired+有旧数据形态丢失卡内一键授权入口，且注释声称的「✦ 入口」不存在

engine 合并（engine.ts ~L300-317）特意把 setup_hint 透传进合并快照，但 AbnormalBody stale-data 分支（metrics 非空）完全不渲染 setup_hint/OneClickAuth——setup_hint 兜了一圈无处落地。对 bl/arkcli 这类 command 通道（auth_expired 正是它们的过期信号），旧行为=整卡文字+「一键授权」直接可点；本卡后若该 provider 有过成功快照，过期后用户看到的是：旧进度条+黄「待授权」徽章+时效标注——无任何重授权动作（刷新只会再次 auth_expired）。OneClickAuth 全仓只在 AbnormalBody no-data 分支渲染，无第二入口。

测试注释（636 行）「重新授权可通过 head 旁边『✦』入口另开」失实——全仓 grep 无该入口。

卡体「不做」写明不动 auth_expired 的 setup_hint+OneClickAuth 流程，而「去掉重复」节又明示「auth_expired 的 lamp + setup_hint 授权引导保留」——语义应是不让引导消失。建议：stale-data 分支对 auth_expired 补渲染 setup-hint/OneClickAuth（与 no-data 分支同构），或确认并落实等效重授权入口后修正注释。非本卡验收项，但属本卡引入的交互回归。

### 3. [engine.test.ts 末例] SUGGESTION — 「errorSnapshot 静态方法带 logo」用例名实不符

该例未触发 errorSnapshot/fetch 抛错真实路径（private static，仅靠喂手工 failSnap 断言 logo 字段），注释「走 fetch 抛错路径触发」与断言错位。logo 传递已由代码读确认（两处调用点传 descriptor.logo），测试可改走真实抛错路径或把注释/命名改诚实。

### 4. [app.css:604-616] SUGGESTION — 新增 raw px 绕过 tokens.css（theme-glass 分支纪律）

`.abnormal-body-stale-note{font-size:11px; margin-top:2px}`、`.abnormal-body-error-reason{font-size:12px}`：tokens.css 已有 `--font-11/--font-12`；间距刻度 4px 起（无 --space-2，即 8px 网格下限），`margin-top:2px` 违反 8px 网格铁律（唯二例外 1px 边框/50% 圆角）。建议改 `var(--font-11)`/`var(--font-12)` + 4px 间距（或确认 --space-4 视觉过重再评估）。

## 已核过（非问题）

- 有旧数据异常卡形态：正常模板+时效标注「当前数据为 N 分钟/小时前采集」+ text-error 错误原因行——正确且 i18n 双语文案到位
- auth_expired 无旧数据整卡：lamp+setup_hint+OneClickAuth+长文案（「待授权」徽章与「登录态过期,请重新授权」长文案并存，非字面重复）
- engine 三态合并 + 失败不落库 + 首次失败整卡落库 + logo 透传——与卡体逐字一致，测试反向对照到位
- storage 历史/TTL、core 通道/适配器零改动；改动面 6 文件全部在 app 内

## 收口要求（最低可验收）

BLOCKING 修复 + WARNING 处理或给出明确设计决定 + typecheck 0 / vitest 全绿 / e2e 关键全绿（重跑落数字）。

---

# Round-2 人工终审（APPROVE）

reviewer: njbx02 (default / 老大人工终审)
reviewed_head: 3a130b965aee46c1a719e6b155c5f9dd42d6effb (origin/feat/theme-glass, 3a130b9)
round-1 意见 commit: 3e94742（已合并入 reviewed_head）
verdict: APPROVE（execution lens；独立复验，非采信 worker 自述）

隔离 clone /tmp/tw-r2-5d8c/repo @ 3a130b9 全量独立复核，亲跑落数字：

## 逐条核验 round-1 意见

| 意见 | 修复落点 | 独立验证 |
|---|---|---|
| 1 BLOCKING — error 整卡双「采集失败」 | ProviderCard.tsx no-data 分支 `showStatusDetail = p.status !== "error"`；测试改文本级 `countBadgeText()`（全卡叶节点 textContent 恰 1，与 class 名无关） | 代码读 ✓；**反向对照独立复验**：临时改 `showStatusDetail=true` → ProviderCard.test 3 failed / 37 passed；恢复 → 40/40 全绿。守卫真咬缺陷，非空转 |
| 2 WARNING — auth_expired+有旧数据失重授权入口 | stale-data 分支对 `auth_expired && setup_hint` 补渲染 setup-hint/lamp/HintCopyButton/OneClickAuth（与 no-data 同构）；测试改断言 setup-hint 存在 + 文本级「待授权」恰 1 | 代码读 ✓（313-322 行，渲染顺序=模板→时效标注→setup_hint→error_reason）；测试断言含 lamp+setup-hint+countBadgeText ✓ |
| 3 SUGGESTION — engine.test errorSnapshot 名实不符 | 改反射调用 private static errorSnapshot，一次性锁契约（logo/status/metrics/error_message/alerts[0].code=adapter_threw） | diff 读 ✓（断言 5 条与签名 (inst,planType,err,logo) 匹配） |
| 4 SUGGESTION — app.css raw px 绕 tokens | `--font-11` / `--font-12` / `--space-4`（margin-top 2px→4px 进 8px 网格） | 代码读 ✓；tokens.css 含 --font-11/--font-12/--space-4 ✓ |

## 独立执行验证（亲跑，非 worker 自报）

- typecheck: `pnpm -r typecheck` 0 错误（core 先 build，隔离 clone 全量）
- vitest: 39 files / **437 tests** 全绿（round-2 净改动 4 文件：ProviderCard.tsx/test、engine.test.ts、app.css；新增 1 反向对照用例 = round-1 436 → 437）
- e2e 关键: smoke + badge-semantics + panel-semantics **13/13** 全绿
- git: reviewed_head = origin/feat/theme-glass = 3a130b9（ls-remote 核对）；净 diff 4 文件 +104/-59，无越界（兄弟 t_4bd214de MCP 改动 884de69 独立于本 commit）
- 文本级守卫语义确认：`countBadgeText` 数全卡叶节点（children.length===0）中 textContent.trim()===badge 的个数——error 卡修复后仅 head badge 1 处；摘守卫立即 3 failed 证明原 round-1 bug 形态（abnormal-status-detail 复渲「采集失败」）确被拦截

## 已核过（非问题 / 本轮边界）

- engine 合并语义（ok 替换 / 失败+有旧 ok 保留骨架仅覆盖 status/alerts/error_message/setup_hint 不落库 / 首次失败整卡 error）与卡体逐字一致，本 commit 未再动 engine.ts 主体
- stale/unsupported 无旧数据卡保留 abnormal-status-detail（statusText ≠ badge，语义互补非字面重复）——设计判断正确
- auth_expired 无旧数据整卡形态不变（lamp+setup_hint+OneClickAuth 保留）
- storage 历史/TTL、core 通道/适配器零改动

## 收口

Round-1 全部 4 项已落地且守卫经反向对照证明有效；typecheck 0 / vitest 437/437 / e2e 13/13 独立复跑通过。无遗留 BLOCKING/WARNING，准予完成。

