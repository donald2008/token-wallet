# 人工审核意见 — t_f9ed9235

reviewer: njbx02 (老大, human 功能验证阶段)
verdict: approved
date: 2026-09-07
head: 0d817e417c86212a6aa6b872ce1bd6f0eb993bfe (feat/mcp-server, ls-remote 核对一致)

## 审核方式

契约透镜独立复核（不采信 worker/auto 自报）：本机 njbx02 全新 clone → 独立装依赖 →
真实起服务 :19131 + 手写 fastmcp client 实测 + 直读 SQLite 对账。

## 实测证据（全部本机真跑）

1. git 证据链：HEAD==0d817e4==远端 ✓；base 含 1a38a93（rebase 已做，
   docs/reviews/t_03bdaf1f.md 在，无假删除）✓；diff 范围==提交声明（21 文件）✓
2. pytest 51/51 独立复跑全绿（含 F1-F6 fixture）✓
3. core 回归 pnpm test 154/154 绿（schema-sql.ts 仅加 source 一列，零回归）✓
4. tools/list 恰为三 spec 工具名（report_usage/usage_summary/usage_report_echo），
   维护入口不在数据面 ✓
5. report_usage 线格式平铺：inputSchema required=["reports"]，无 payload 包装层 ✓
6. 5 条混合（2好+1重复 event_id+坏 status 枚举+坏 uuid）→ accepted=2/duplicated=1/rejected=2，
   计数不变量成立，rejected 逐条错误明细正确 ✓
7. F6 交叉守卫过线实测：completed+usage:null → rejected（终审 P2 #2 硬性要求）✓
8. usage_summary vs echo 对账：total.calls=2，分 agent tokens（2045/710）与原文逐条
   重算一致；timezone=Asia/Shanghai（daemon 本地 IANA 名，§2.2）✓
9. 混币种拆行：同 agent USD/CNY 各一行，total.cost_total=null ✓
   （副产发现：fingerprint 不含 cost/currency，仅差 cost 的两事件被二级判重吞——
   核对 spec §3 canonical 字段确认设计如此，§3 误杀声明已覆盖，非缺陷）
10. 无 key → 401（loopback 不豁免）✓
11. TTL 先聚合后删强证明：ttl=90 时昨日窗聚合 tokens=miss+out（2045/710，cache_hit 9999
    不入列 ✓）、100 天旧事件删、今日留；再以 ttl=1 触发 → 昨日原始删除而聚合行仍在，
    证明顺序=先聚合后删、原始可删数据不丢 ✓
12. echo limit>500 拒绝；echo 每条附 _computed.fingerprint ✓
13. systemd user unit（Restart=on-failure/RestartSec=30/TimeoutStopSec=180 照
    kanban-mcp-server 模式）+ README 部署节（Consul key 注入、key 不落 unit）✓
    Consul 服务注册顺延已显式落档（README 部署节）✓
14. spec 回写同批：§2.2.1 交叉守卫 + §4.2 tokens=miss+out 修正 + §8 F6 ✓

## 三轮 auto 审查追溯

- R1（qwen3.8-max）8 条 / R2（deepseek-v4-flash）8 条：本轮抽查关键项均实证落地
  （时区契约、raw_json 原文保护、wire 平铺、混币种、_computed 钉住）
- R3（k3）APPROVE，无新增问题；本 human 阶段复核结论一致

## 修改项

无 BLOCKING / WARNING / SUGGESTION。

## 备注（非本卡范围，记档）

- spec §4.3 语义确认：每日维护只聚合「前一天」，TTL  purge 假定历史已被日常聚合覆盖；
  daemon 停机 >TTL 天的极端场景旧明细会未聚合即删——spec 设计如此，不过度设计。
- 部署到 njbx02 由老大另行执行（systemd unit + Consul key + 服务注册落地）。
