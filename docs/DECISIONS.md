# token-wallet 决策记录

格式: D-NNN | 日期 | 决策 | 理由 | 状态

| ID | 日期 | 决策 | 理由 |
|----|------|------|------|
| D-001 | 2026-08-27 | 全 TS 技术栈: React 19 + Vite, 桌面壳仅作窗口壳(壳选型后被 D-033 推翻改 Electron) | 团队 React/RN 延续性零学习成本; 复杂度在接口对接不在系统编程; Svelte 包体优势对部件应用无足轻重 |
| D-002 | 2026-08-27 | 图表: Chart.js v4(数据序列) + 手绘 SVG(状态微部件); 不引 echarts/组件库 | echarts 按需引入仍 150KB+ 对部件太重; Chart.js v4 是现代重写版(ESM/TS/tree-shake); 微部件手绘仅几十个 SVG 元素 |
| D-003 | 2026-08-27 | 部件形态 A+B: 托盘+弹出面板为主, 悬浮窄条可选, 可最小化到托盘 | 托盘色点回答"有没有事"是 glanceability 极致; 窄条满足零点击可见 |
| D-004 | 2026-08-27 | Template(视觉形态)与 Theme(配色)分层; 模板注册进 registry, MVP 先做 bars+ticker | 用户要求多套模板且不限于颜色变化; registry 发现机制便于开源贡献 |
| D-005 | 2026-08-27 | 数据面 cache-first / stale-while-revalidate; UI 永不直连 provider | 启动即出数/断网可用/历史速率计算; status 一等公民表达 stale/auth_expired/unsupported |
| D-006 | 2026-08-27 | 持久化: StorageBackend 接口先行, MVP JsonlStore, P3 评估 SqliteStore | 双宿主约束(webview 无 Node API); 等 quota_history 查询需求明确再上索引, 先实现再抽象 |
| D-007 | 2026-08-27 | monorepo 三包: core / app / mcp-server; mcp-server 定位=常驻数据面 daemon 而非单纯 Agent 接口 | 桌面 app 关闭不影响采集; 一套代码三种部署形态 |
| D-008 | 2026-08-27 | Agent 对接唯一通道 = MCP(streamable-http) | 复用 kanban-mcp 鉴权经验; 不维护第二套 HTTP API |
| D-009 | 2026-08-27 | 通知后置 P3, 配置项默认关, Notifier 接口 P0 先行(空实现) | 优先跑通核心功能; 接口先行使 P3 不动数据面 |
| D-010 | 2026-08-27 | 主题深浅双套 CSS 变量, 默认追随系统 | prefers-color-scheme + 桌面壳原生 API |
| D-011 | 2026-08-27 | 轮询 per-provider 可配置, 默认 T2 档 5min | 额度数据无推送通道, 实时上限=轮询; T3 控制台类 30min+ |
| D-012 | 2026-08-27 | Apache-2.0; gitee 主仓(ITEater/token-wallet), v1 稳定后 GitHub 镜像 | 用户偏好 gitee 管理, 国际化后置 |
| D-013 | 2026-08-27 | 凭据 CredentialSource 接口: env/file/command; 仓库不落任何密钥 | 开源项目不假设 Consul; 我们部署用 command 接 Consul KV |
| D-014 | 2026-08-27 | 三原型建模: balance / window / local; 统一 ProviderSnapshot schema | 覆盖 coding plan 窗口制/余额制/本地用量三类典型 |
| D-015 | 2026-08-27 | 框架边界: 四个注册点(ProviderAdapter/Template/CredentialSource/Notifier), 不做插件市场不做热加载 | 防过度设计; 加平台=发新版 |
| D-016 | 2026-08-27 | 语义色拆双 token: --warn(填充/图形) 与 --warn-fg(文字) | 浅色主题下亮黄填充上文字必须深色, 共用单一色值会把黄色压成棕色; 同一语义不同角色不同色值, ok/bad 同理预留 |
| D-017 | 2026-08-27 | 平台对接=预置通道(channel)+参数录入; 用户不接触 YAML/JSONPath; 设置页动态表单+测试连接; custom-http 高级通道后置 | 映射规则是通道内部实现细节不是用户输入面; 录入即验证(测试连接) |
| D-018 | 2026-08-27 | 适配器实现四类: http / command(包装官方 CLI) / session(Cookie) / local-agent; 配置三层分离(内置通道目录/实例/全局设置); 凭据统一 CredentialRef(store/env/file/command) | 由 7 家实战收敛; aliyun 走 bl CLI(command 类首实例), api_key 保留用于探针模式; longcat 暂缓 |
| D-019 | 2026-08-27 | 存储位置按平台解析(桌面壳主进程 path API / env-paths), 零硬编码; 配置(Roaming)与数据(Local)分家; 凭据进 OS 钥匙串, headless 降级 600 文件 | Windows 为主平台, Roaming 漫游同步不应拖快照大文件 |
| D-020 | 2026-08-27 | 持久化转正 SQLite 为 P0 主力(推翻 D-006 的 JSONL-first): app 走主进程 sqlite IPC(壳随 D-033 换 Electron), mcp-server 走 node:sqlite; JsonlStore 降级为调试导出 | 每时段×每模型消耗记录是核心需求, 聚合查询需要索引, JSONL 全扫扛不住 |
| D-021 | 2026-08-27 | 分发=标准 Windows 安装包(NSIS); 首开向导=隐私声明(零遥测, 须同意) → 引导添加首个 provider; 初始零配置 | 用户明确定调标准安装形态 |
| D-022 | 2026-08-27 | 状态阈值全局可配置(黄线默认 30%, 红线默认 10%); error/耗尽恒红不走阈值; auth_expired 定黄(见 §2.1) | 用户要求阈值入配置 |
| D-023 | 2026-08-27 | command 通道依赖由 app 内一键安装(bl 走官方二进制脚本 irm|iex, 无需 Node), 安装过程 stdout 实时流入 log 抽屉 | 用户要求安装进度可视化; bl 官方提供免 Node 二进制安装 |
| D-024 | 2026-08-27 | 托盘/应用图标为自设计 token-wallet logo; 开机自启开关默认关; mcp-server 同机本地部署; local-agent 通道 v1 仅预留占位 | 用户定调; MCP 设计后置 |
| D-025 | 2026-08-27 | 通道两层模型: platform → product(同平台多计费产品: coding plan/token plan/按量); 添加流程两步(选平台→选产品→填参数); 同平台实例面板可聚合 | 单层通道无法表达"同一平台多种计费形态"; 用户指出 |
| D-026 | 2026-08-27 | 实例多账号: 同产品允许多实例; name 必填全局唯一, 默认"<平台>-<产品> #N"自动编号; 表单+加载双重校验; 卡片标题显示实例名 | 同一平台不同账号(api-key)需区分, 用户指出 |
| D-027 | 2026-08-27 | 调度器全异步并发: 实例独立循环/防重叠/超时硬切断/启动抖动/失败指数退避/auth_expired 停摆 | 单实例故障零影响他人; 防风控叠加 |
| D-028 | 2026-08-27 | 采集方式收敛为两类: http(API 直调) + command(官方 CLI 包装); session/控制台 Cookie 模拟类整体移除(含阿里 Cookie 兜底) | 官方 CLI(bl/arkcli)已覆盖原需 Cookie 的平台; 少一类少一份会话维护与风控风险; 用户定调 |
| D-029 | 2026-08-27 | 凭据存储: 桌面端一律 OS 钥匙串(Windows 凭据管理器/macOS Keychain, keyring crate); headless 降级链 env → command(Consul) → 600 权限文件(文档显著警告)。内存纪律: key 只活在请求构造瞬间, 日志统一出口模式脱敏, 数据目录 0700, 删实例同步删钥匙串条目 | 威胁模型: 同机进程读文件 > 误提交/误贴 > 备份上云; 主密码加密文件体验不可接受 |
| D-030 | 2026-08-28 | 前端 E2E = Playwright browser 模式: headless Chromium + mock 桌面桥 IPC, Linux/CI 可跑全平台(D-033 起为自家 harness 注入 window.tokenWallet) | 官方 WebDriver 方案配置繁琐/flaky 社区差评多; browser 模式无 Windows 依赖, 与 visual-test 同构; 真壳 E2E 后置(Electron 壳记 P2) |
| D-031 | 2026-08-28 | 安装实施: 无代码签名(P4 前), Windows 本机构建唯一渠道, 自动更新后置(更新=重装), gitee release 挂安装包 + SHA256(壳随 D-033 换 Electron, 打包链 E3 重建) | 签名/updater 是 P4 商业化/发布项; Linux/WSL2 无法可靠 cross 出 Windows 包; 不为低频发版维护 Windows gateway |
| D-032 | 2026-08-28 | instances.yaml 读写走主进程 IPC(YAML 解析/生成, tmp+rename 原子写), IPC 传 JSON, 前端零 YAML 依赖、zod(schema.ts)仍是唯一校验权威; consent 首开判定落 configDir/settings.json(配置侧) | webview 无 fs 权限; 前端引 YAML 库违背零冗余依赖; 配置/数据分家(D-019): instances.yaml+settings.json 进 configDir, 快照 sqlite 留 dataDir |
| D-033 | 2026-08-29 | 桌面壳从旧 Rust/Webview 壳换 **Electron**(旧壳废弃删除不过渡); core(纯 TS)全复用产品逻辑零重写; IPC 通道名契约保全(get_bootstrap/instances_load/instances_save/record_consent/keyring_*/http_get_json/sqlite_*/get_storage_paths/update_tray_status + 新增 win_minimize/win_close); renderer 经 preload contextBridge 注入 window.tokenWallet.invoke; 持久化语义随壳移植(原子写 tmp+fsync+rename 覆盖、consent RMW 保留未知字段, f318b3c W1/W2/W3 不退化); keyring/sqlite/http 本卡显式降级(E2 接真, 面板出错误卡是预期); 打包链 E3 重建 | 诱因①旧壳依赖链过重(本机实测: webkit2gtk/gtk3/appindicator/rsvg/xdo 五包 + cargo 需 edition2024 必须 rustup 1.98); ②Windows 窗口层受限(P0-9 整卡在与 DWM 搏斗); 内存/包体不是考量。E1 实测证据(2026-08-29, desktop-e5jupfs): Electron 37.10.3, `pnpm dev` 起壳 = 无边框透明窗(xdotool 可查 token-wallet 窗) + 托盘 nativeImage 四态 + 单实例锁(二次启动即退 exit 0) + get_bootstrap 走真实 ~/.config/token-wallet/settings.json(日志实证 firstRun=true); core 61 / app 30(含新增主进程 8 条原子写+consent RMW 单测, 移植自旧壳 3 个 Rust 测试语义) / e2e browser-only 24 全绿 |
| D-034 | 2026-08-29 | app 主进程 SQLite 统一 **`node:sqlite`**(`DatabaseSync`), 弃用 better-sqlite3(dependencies + @types + esbuild external 声明一并清除, 不留过渡); 数组行取数用 `stmt.setReturnArrays(true)` 替代 better-sqlite3 `.raw()`; PRAGMA 走 `db.exec("PRAGMA ...;")` 替代 `db.pragma()`。IPC 契约(sqlite_batch/exec/query 语义)与主进程/renderer 分层不变, SCHEMA_SQL 单源(D-020)不变 | 原生模块一份 .node 服务不了双 runtime: Electron 37 需 ABI 136 而 vitest 在 Node 22 需 ABI 127, 壳能跑则单测挂(electron/sqlite.test.ts 9 例 `NODE_MODULE_VERSION 136 vs 127`), 单测能跑则壳挂; 且仓库零 rebuild 固化(无 .npmrc/postinstall/@electron/rebuild), 那份 ABI 136 二进制是手动一次性产物, 换机即失效。改用内置模块消灭的是问题类别而非单个 bug: 无原生模块 → 无 rebuild 前置 → 无 ABI 冲突 → Windows `pnpm install` 一步即可跑 → E3 electron-builder 免 asar unpack native; 并与 core 统一(D-020 起 core/mcp-server 已用 node:sqlite), 消除同一 monorepo 内两套 SQLite 实现。njbx02 实测(Electron 37.10.3 / Node 22.21.1): node:sqlite 开箱可用**零 flag**; `setReturnArrays(true)` 返回列序数组且**重复列名不丢列**(`Object.values` 方案会丢, 已否决); BLOB 直接返回 Uint8Array(省一次 Buffer 转换); 错误结构化 `ERR_SQLITE_ERROR`; `run()` 返回 `{changes,lastInsertRowid}` 语义对等。代价 = 接受 node:sqlite experimental 标签(仅 ExperimentalWarning, 不依赖任何启动 flag), 与 core 已承担的风险同级 |
| D-035 | 2026-08-29 | **Windows 打包链落地 = electron-builder NSIS**(E3): `appId=com.tokenwallet.app`(沿用旧 Tauri identifier)、`productName=token-wallet`、artifact=`token-wallet_<version>_setup.exe`; nsis **oneClick 默认静默安装**(支持 /S) + **单 exe 全离线**(无运行时下载) + **无签名**(D-031 不变); 主进程 esbuild bundle + 渲染层 vite `base:"./"`(file:// 加载必须相对路径), asar 内仅 dist/dist-electron/electron-icons; 一键脚本 `scripts/build-windows.ps1`(自动装依赖→pnpm install→pnpm -r build→electron-builder→SHA256 落 `.sha256` 文件, 大陆镜像自动注入 ELECTRON_MIRROR/ELECTRON_BUILDER_BINARIES_MIRROR); 产物挂 gitee release 渠道不变(D-031); 根 scripts 增 `build:win` | 构建渠道 Tauri→electron-builder 是 D-033 换壳的直接延续。**依赖链收窄事实(E3 验收点名)**: 构建机从「Node + Rust stable(rustup 1.98) + VS Build Tools(MSVC) + WebView2 工具链」收窄为「仅 Node.js 22+ + corepack/pnpm」——无 Rust 编译、无 C++ 工具链、无原生模块(配合 D-034 node:sqlite 归零 ABI/rebuild 前置) → 任意 Windows 机 clone+跑 ps1 即出包。E3 实测(WSL2 Linux 侧验证构建, Electron 37.10.3): electron-builder NSIS 目标可在 Linux 原生出包(免 wine 需 `signAndEditExecutable=false`, 仅资源嵌入差异; Windows 本机构建走 ps1 全量); 渲染层 base './' 修复前 `dist/index.html` 的 `/assets/*` 在 file:// 下解析到盘符根 → 真机白屏风险, 已修。**2026-08-30 补记 · 真机实证放宽构建机前提**: 2026-08-29 用户在 Windows 真机安装了 **WSL2 侧 electron-builder 产出**的 NSIS 包(`token-wallet_0.1.0_setup.exe`, SHA256 `50ad1f8f…`), 实测可安装、可运行、首开 consent 正常、设置页可添加真实 provider key(由此发现双源缺陷开出 t_44497e20)、整体观感通过。据此: D-031 当年"Windows 本机构建是唯一可靠渠道"的判断属 **Tauri 时代结论**(Rust/MSVC/WebView2 交叉编译不可靠), 在 Electron 下**已被实证放宽**——WSL2/Linux 出的包对 Windows 真机可用, 原生 Windows 从"必需"降为"发版首选"(签名/图标资源嵌入等差异仍以 Windows 本机 ps1 全量构建为准)。**仍未真机验证的部分**(不在本条实证范围): 托盘四态、无边框透明圆角观感细验、单实例锁、重启存活、开机自启、卸载干净 |
| D-036 | 2026-08-29 | **通道目录单一真相源 = core PRESET_CHANNELS + CHANNEL_MAPPINGS, 禁 app 侧 mock 通道树**; 映射能力三扩展: `MetricMapping.unit` 增 `percent`(opencode 只给 percent / kimi limit 恒 100 归一)、新增 `iso_epoch` pipe(ISO 8601 → unix 秒, 容忍毫秒小数 `.879Z`/`.687248Z` 与 Z/偏移时区)、`FieldMapping.const`(常量值如 opencode limit=100, 与 path 二选一, 禁"映射到不存在路径再兜底" hack); 单窗 status 不污整卡(通道映射零 ok_assertions, opencode weekly rate-limited 由 metricHealth+bars 最紧窗口自然判红, D-022); 不变量由单测保证 `PRESET_CHANNELS ⊆ CHANNEL_MAPPINGS`(选得到即采得到) | 用户真机 E3 实测: 设置页加了 opencode key 面板却显示"暂未接入" —— 根因不是适配器没写, 是**双数据源不一致**: 设置页树读 app 侧 `mockChannels.ts`(列了一堆平台), 真实通道目录 core `presets.ts` 只有 deepseek → 用户选了、填了 key, 采集层兑不了现。P0-8 的 unsupported 显示语义工作正常, 真正缺陷是设置页在承诺产品不具备的能力; 只补适配器不切数据源 → 下一个通道继续骗人。切单一真相源后, "列出的都能真采集"由目录⊆映射不变量保证(新增通道必须两处同时注册) |
| D-037 | 2026-08-30 | **卡间排序 = key(名称\|紧要度)×dir(正排\|倒排) 两正交参数, 缺省名称正排**(t_c31e6099 #829 R1, 推翻 §6.1"最坏情况优先置顶"为唯一默认); 紧要度=卡内最紧窗口剩余比例升序, 健康度三级序降级为并列稳定键; 配置整体 `{key,dir}` 持久化 settings.json(新 IPC get/set_sort_config, RMW 同款)/浏览器降级 localStorage `token-wallet.sortConfig.v1`; 托盘全局最差状态不受排序配置影响。同卡附带: 重置倒计时改单单位一位小数(#829 R2, ≥1天 X.X天 / <1天 X.X小时 / <1小时 X分, 纯倒计时无动作后缀, 倒计时列定宽右对齐); 设置弹窗头固定+内容区内滚不穿透(#829 R3, overscroll contain 移到 .settings-body) | 2026-08-30 老大真机三轮反馈: ①名称正排是最不易误读的默认, 紧要度应为可选而非默认; ②"26天1小时"组合文案长且列宽漂移, 单单位一位小数更稳; ③弹窗滚到底背景跟滚+头部随滚动消失, 影响取消/× 命中 |
| D-038 | 2026-08-30 | **信息架构改造 = 操作按作用域分区**(左侧 44px 窄侧栏承全局动作「添加/刷新/设置」; provider 卡 head 右上删除钮承实例动作, 点击弹既有 confirm-delete 气泡; 设置弹窗瘦身为纯偏好页「主题/排序/开机自启/存储路径」, 移除添加入口与实例列表); 标题栏瘦身只留 app-title + 图钉/最小化/关闭, **hover 显隐逻辑整体移除**(全部常显, 图钉不再有常显特判), 主题切换钮删除(入口只留设置页既有分段控件, 不新增控件); 添加向导抽为独立组件 AddProviderWizard, 双入口(侧栏 ＋ 弹窗 / 空态大按钮页内引导 D-021 不变), 保存成功即回面板不再回落实例列表 | provider 余额展示是主体, 低频操作不该占标题栏宽度预算(360px 下六钮曾把标题挤到断词, t_2ac39613); 删除就近放卡片 = 所见即所改, 比"进设置找列表再删"少两跳且不易删错; 侧栏常驻 44px 换来标题栏单行常显与卡区完整宽度; 分区后每个位置一职, 设置页回归"偏好"语义 |
| D-039 | 2026-08-30 | **卡片拖动排序(手动优先级) = 拖动即切 manual + order 持久化 + 实例集为真相源**(t_abd4f11e); SortConfig.key 增第三值 `manual`(normalizeSortConfig 接受, dir 强制 asc; order?: string[] 为附加信息), sortProviders manual 按 config.order 与当前实例集合做交集(新添加/历史漂移 id 按缺省规则名称正排追加尾部, 幽灵 id 忽略, 不因 order 过期丢卡); 拖动=用户接管排序(任何模式直接拖卡立即切 manual), 设置页排序控件三档(名称/紧要度/手动), 切到名称/紧要度时 order 保留不清(再切回手动恢复); 拖动过程只更新内存 state, **松手(drop)才持久化一次**(settings.json sortConfig 整体 RMW + 浏览器降级 localStorage, 快速连续拖动以最后一次 drop 为准, 原子写 tmp+rename 已保证文件级一致不额外造锁); 拖动手柄=卡片 head 左侧 16px 品牌色块(cursor grab/grabbing), 实现选 **pointer events**(非 HTML5 DnD: Electron webview drag image 行为不稳, Playwright mouse API 天然产生 pointer 事件, jsdom 可单测), 视觉=被拖卡浮起(shadow 提升+transform 跟手)+落点指示线(drop-line 绝对定位)+松手归位(CSS transition 0.18s); 与引擎无交互(instancesKey 不含排序, 纯前端展示层), manual 模式全局健康托盘逻辑不变(globalHealth 与排序无关) | 产品决策(2026-08-30 老大): 手动优先级与配置排序互斥可切换, 核心原则「拖动 = 用户接管排序, 引擎排序 = 配置驱动」; 零配置成本(拖了就生效)与持久化恢复(切回手动不丢)是两个硬验收; 不引拖拽库(D-002 手绘精神) |
| D-041 | 2026-08-30 | **aliyun-bailian/token-plan command 类首实例 + Windows spawn 适配**: ①通道 descriptor adapter=`command`, plan_type=`window`, **params_schema=[] 零录入**——console 会话由 bl CLI 自管(`~/.bailian/config.json`), app 不碰凭据文件; DESIGN.md 通道目录里 params:{api_key} 的探针模式**后置**(不进本卡); ②采集 `bl usage token-plan --output json`: `per1WeekPercentage` 是 0-1 小数(0.379=37.9% 已用)归一 ×100 作 percent、`per1WeekResetTime` 毫秒→秒存 reset_at, 5h 窗字段缺席=正常态不报错; ③异常分类: exit=3 + `No console access token found`/`not logged in or has expired`/`NotLogined` → auth_expired(黄灯, setup_hint `bl auth login --console`); `bl auth status` exit code 不可信(未登录也 exit=0)一律解析 body; spawn ENOENT → error 态 setup_hint 指向 D-023 安装; ④Windows spawn 适配进 ScriptedAdapter.runCommand 封装(caller 无感): `.cmd` shim 被 Node≥18.20 拒(CVE-2024-27980)→ win32 一律 `cmd /c <cmd> <args>` + `windowsHide:true` 防黑框闪烁; **round2(#862)补 shell 包装 CLI 缺失分类**: win32 包壳使 spawn ENOENT 结构性不可达(cmd.exe 恒存在), bl 缺失时 cmd 非零退出+stdout 空+stderr 判别串(`not recognized`/`不是内部或外部命令`)→ 与 ENOENT 同判 error+安装 hint; CommandRunResult 带出 stderr(脱敏: 仅供分类不进 error_message); 分类不依赖具体 exit code(契约说 exit code 不可信, 9009 仅示例); healthCheck 同场景 ok=false+安装 hint; 决策二选一取 stderr 判别(弃 %APPDATA%\npm\bl.cmd 全路径探测: 只覆盖 npm 全局安装位, stderr 判别通吃任意安装方式); ⑤console access_token 过期是服务端黑盒不做本地预测, 采集失败即健康信号; **round3(t_c561c8a8 终审 BLOCKING)补**: 真实 bl 1.18.x 未登录时 `bl usage token-plan --output json` exit=3, 但错误 JSON 写 **stderr**、stdout 空 —— auth_expired 判别必须覆盖 stdout+stderr(`isAuthExpiredBody(res.stdout + res.stderr)`), 否则判别永不命中落通用 error 卡; 单测补 stderr 形态 fixture(保留 stdout 用例, 双 stream 都要能判), healthCheck 同口径 | 2026-08-30 三兄弟对齐后本卡决策(全部实测取证, 不猜接口): bl 1.18.1 Windows 真机 golden sample 已拿到; 探针模式(api_key 查 usage)已证伪三次, 不做 API key 录入面; command 首实例把 spawn 平台分支沉淀进 runCommand, 后续 arkcli 等 command 通道复用 |
| D-042 | 2026-08-30 | **command 通道引擎接线 = COMMAND_ADAPTERS 解析 + 主进程 command_run IPC 桥**(t_c561c8a8, D-041 known_followup): ①引擎 buildInstances 按 descriptor 分流——adapter=="command" → COMMAND_ADAPTERS[channel](core channels/aliyun-bailian.ts), http 维持 CHANNEL_MAPPINGS 原路径, 两者都缺 → 显式 unsupported(P0-8 防御保留); ②**spawn 强制走主进程**: 新增 IPC 通道 command_run(名单登记进 DESIGN.md §5.0.2, D-033 搬迁不变量), 主进程 handler 内 COMMAND_ADAPTERS[channel]() 构造**缺省 runner(真实 spawn)** 适配器执行 fetchSnapshot 返回 ProviderSnapshot, renderer 侧 ipc.ts commandRun() 经 window.tokenWallet.invoke 传输(零 Node 能力, P0-4 同族); ③**不注入假 runner**: 生产路径全程真实 spawn, L1 测试注入 executor 仅覆盖载荷校验/透传, 真实 spawn 取证在 electron/command-run.test.ts(本机 bl 未装 → error + 安装 hint, 验收 2 真实路径证据); ④testConnection command 分支同步接真(走同一 command_run 桥), 删除 mock 兜底快照——bl 未装时用户看到「bl CLI 不在 PATH, 请安装后重启应用」而非假成功; ⑤浏览器 dev 无桌面桥 → commandRun 返回 null → 引擎转显式 error 快照(「需桌面壳执行」, 语义≠unsupported); ⑥renderer 可安全 import COMMAND_ADAPTERS 判断注册(vite 实测可打包, node:child_process 是动态 import 被 externalize 仅警告, 只读不调用) | D-041 交付时已知缺口: renderer 静态 import node:child_process 不可行, 适配器有真实现但引擎不认 → 用户添加 aliyun-bailian/token-plan 实例落 unsupported 卡, 违反 D-036「选得到即采得到」。主进程桥与 http_get_json 桥同构, 是换壳(Electron)后唯一正确的 command 执行路径; 真机真实数据(需 bl console 会话)归用户 Windows 人肉验收清单, 本卡交付到「引擎能调真实适配器 + 缺 CLI 正确报错」 |
| D-044 | 2026-08-31 | **volcengine-ark/coding-plan command 类第二实例(arkcli, t_356da53d)**: ①descriptor adapter=`command`, plan_type=`window`, **params_schema=[] 零录入**——控制面会话由 arkcli 自管(volc-sso SSO), app 零凭据; ②采集 `arkcli usage plan --format json`(auto-discover 4 SKU, 适配器内筛 `product=="coding-plan" && subscribed`): 响应 items[].periods 的 **period label = session(5h)/weekly/monthly**, **`percent` 已是 0-100 百分数(勿 ×100, 与 bl 的 0-1 小数 D-041 相反)**, **`reset_at` 是 RFC3339 字符串(含 +08:00, Date.parse→秒; bl 是毫秒 epoch)**, percent 缺席时 used/total*100 兜底; ③异常分类: exit=1 + error body `{ok:false,error:{message}}` 写 **stderr**、stdout 空 → auth_expired(黄灯, setup_hint `arkcli auth login volc-sso --no-browser`); **auth status 未登录也 exit=0, REAL 判别字段是 `logged_in`(非 task 假设的 `ok`字段)**, healthCheck 解析 `logged_in===false` 判"从未配置"; `--api-key` 对 usage 无效(已证伪); ④COMMAND_ADAPTERS 注册表进 **aliyun-bailian.ts 全局常量追加条目**(非新通道自建), spawn/三态分类复用 D-041 win32 `cmd /c` 包壳 + isShellCommandNotFound + ENOENT 路径, 零新基建; ⑤前端 DynamicForm 加通用**两段式 SSO 授权指引**(command 通道 params_schema=[] 无可挂 help 字段), CLI 名从 `health_check.command` 首 token 动态取(勿硬编码某通道包名) | 火山方舟 Coding Plan 由官方 arkcli CLI 查询, 控制面 SSO 会话 CLI 自管; 响应结构由官方 binary 内嵌 skill 文档 + 干净 HOME 实测 + pi-ark-quota 三方取证(非猜)。真实响应 shape 与 bl(bailian)差异大: percent 单位相反、reset_at 格式相反、period label 命名相反(bl 用固定 5h窗字段, ark 用 session/weekly/monthly 命名窗)——每个差异都实测钉死。任务卡假设"auth status 解析 ok 字段"被实测推翻 → 按 reality 用 logged_in(汇报注明)。编号 D-044 因 t_65241a9e(D-043 key 去重)与 zai(GL M Coding Plan D-043)已先占 D-043, 决策编号查重纪律避让 |
| D-045 | 2026-08-31 | **智谱 zai/coding http 通道(双窗口绝对值制, t_3c0fe7c9)**: ①descriptor platform=`zai` product=`coding` plan_type=`window` adapter=`http`, 端点 `GET https://open.bigmodel.cn/api/monitor/usage/quota/limit` + Bearer 套餐 key(与 coding 推理 key 同一个); ②**HTTP 恒 200, auth 状态在 body.code → GenericHttpMapping 新增 `body_code` 判态**(与 `auth_expired_status` 互斥): 先判 `auth_expired [401]`, 再 `ok [200]`, 其余(如 1001 缺头)→ error(`error_message: 业务码 <n>`); zai 不设 auth_expired_status(HTTP 判不了); ③**双窗口按位置下标映射** limits[0]=unit:3,number:5(5h窗)/limits[1]=unit:6,number:1(周窗), **绝对值制 used=currentValue、limit=usage、unit=credits 直接进, 不 ×100**(区别于 aliyun 0-1 小数 percent); ④reset_at 走新增 `ms_epoch` pipe(毫秒 epoch /1000→秒, 对 ISO 用 iso_epoch 会拿到毫秒级错误值); ⑤setup_hint 覆盖 auth_expired 卡复制钮语义(ProviderCard 既有反引号提取+clipboard 渲染); ⑥aggregate `level` 记 P2 暂不展示 | 智谱 GLM Coding Plan 配额端点实测取证(非猜): 旧 `/api/paas/v4/balance` 余额端点已下线证伪不再试; 真 key(Consul zai-key)双窗出真数 5h 377/2000 + 周 6837/10000(ms_epoch floor); 坏 key HTTP 200 + body.code=401「令牌已过期」→ auth_expired+setup_hint; 缺 Authorization body.code=1001 → error。三态 fixture(HTTP 皆 200)真咬判态缺陷。**编号避让: D-043 归 t_65241a9e key 去重、D-044 归 volcengine-ark, 本卡取 D-045**(决策编号查重纪律) |
| D-046 | 2026-09-01 | **自动更新 = electron-updater@^6 generic 通道**(自家 nginx 托管 `http://10.200.1.88:8889/token-wallet/`, `build.publish` 声明), 触发模型借 hermes update 模式: **启动静默 CHECK ONLY**(`autoDownload=false`+`autoInstallOnAppQuit=false`, 只发现不下载) + 设置页关于区显式四态(当前版本来自 get_bootstrap / `检查更新`←up-to-date / `更新到 vX`←available / `正在下载 N%`←downloading 事件推送 / `重启安装 vX`←ready), 下载与安装永远用户点击触发; 主进程新增 IPC `updater_check`/`updater_download`/`updater_install`(DESIGN §5.0.2 名单), 进度走 `webContents.send(updater_event)` + preload `onUpdaterEvent` 事件桥(invoke 只覆盖请求响应); 完整性 = latest.yml SHA512 内建校验 + blockmap 差量(nginx Range 已实证 206), **不做代码签名**(D-031 边界不变), 更新源不做 UI 配置项; dev(`app.isPackaged=false`)三通道恒 `unavailable` 显式态不假装; 纯逻辑沉淀 `electron/updater.ts`(AppUpdaterController, 依赖注入 fake 可测, node vitest 9 例); **版本纪律(新)**: packages/app/package.json version 与 git tag 同号, 本次 v0.2.0 = 最后一个手动安装版(bootstrap), 发版三件套(exe+latest.yml+blockmap)传 `/mnt/www/tts-test/token-wallet/`; gitee/github provider 开源(P4)再议 | v0.1.4 真机复验通过后用户拍板自动更新先行(2026-09-01)。electron-updater 官方通道零自研更新器; hermes update 的「自家托管+手动触发+数据不动」三原则平移; NSIS `deleteAppDataOnUninstall:false` + userData 目录稳定 → 更新不丢实例数据; nginx Range 实测 206 才定的 generic+blockmap 差量 |
